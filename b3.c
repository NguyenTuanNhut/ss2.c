#include <stdio.h>
int main(){
    int so1=5;
    int so2=10;
    int s,tich,tru;
    float thuong;
    s=so1+so2;
    thuong=so1 / so2;
    tich=so1 * so2;
    tru= so1 -so2;
    printf("tong cua 2 so la: %d",s);
     printf("thuong cua 2 so la: %f",thuong);
      printf("hieu cua 2 so la: %d",tru);
       printf("tich cua 2 so la: %d",tich);
    return 0;
}